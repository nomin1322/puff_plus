# Context (current)

Milestone: Day 4 — 3 obstacle families + logging stable.
Last verified: py -m compileall . ; py src/main.py
Next step: analysis script + baseline vs personalised comparison.
Milestone: Git + AGENTS + CONTEXT + RUNBOOK in place; line endings normalised
Next step: analysis script that auto-generates 1 chart + 1 table from data/runs.csv
Last verified: the exact commands you just ran (git status, plus your game run command)